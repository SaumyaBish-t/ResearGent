# Base URL of the ResearGent FastAPI backend (the `researgent serve` process).
# The browser connects directly to this origin for the SSE stream, so it must
# be reachable from the client and listed in the backend's CORS_ALLOW_ORIGINS.
NEXT_PUBLIC_API_BASE=http://localhost:8000
