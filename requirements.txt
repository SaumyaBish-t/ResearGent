# Production deps only — pip resolves transitives.
# Heavy local-ingest packages (sentence-transformers, gliner) excluded
# because ENABLE_LOCAL_RETRIEVAL=false + AUTO_SAVE_TO_NOTES=false in prod.
# Their callers already have try/except fallbacks.

# --- API + middleware ---
fastapi>=0.115
uvicorn[standard]>=0.32
sse-starlette>=2.1
starlette>=0.41
itsdangerous>=2.2          # SessionMiddleware

# --- Auth + billing ---
authlib>=1.7
pyjwt[crypto]>=2.10
httpx-oauth>=0.16
razorpay>=2.0

# --- Config ---
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# --- LLM clients ---
openai>=1.55               # used for every OpenAI-compatible provider
httpx>=0.27

# --- Agent graph + DB ---
langgraph>=0.6
langgraph-checkpoint-postgres>=2.0
langgraph-checkpoint-sqlite>=2.0    # local-dev fallback path
psycopg[binary,pool]>=3.2
sqlalchemy>=2.0

# --- Retrieval / web / papers ---
tavily-python>=0.7
ddgs>=9.0
arxiv>=2.1
pypdf>=5.0
pymupdf>=1.24

# --- Utilities ---
numpy>=2.0
tiktoken>=0.8
rank-bm25>=0.2.2
pyyaml>=6.0
rich>=13.9
typer>=0.13
